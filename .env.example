# AI Gateway Configuration

# Domain (required for SSL)
DOMAIN=aichat.kashifkhan.dev

# Server
PORT=8090

# Authentication
AUTH_ENABLED=true
API_KEY=sk-your-secret-api-key-here

# OpenCode Backend (runs on host, not in Docker)
OPENCODE_HOST=host.docker.internal
OPENCODE_PORT=3001

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_RPM=60
